apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-application.fullname" . }}-test-connection"
  labels:
    {{- include "ai-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
