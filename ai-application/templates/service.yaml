apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-application.fullname" . }}
  labels:
    {{- include "ai-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-application.selectorLabels" . | nindent 4 }}
